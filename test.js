// Define some functions and variables
const greet = () => {
  return "Hello, world!";
};

const calculate = (a, b) => {
  return a + b;
};

const settings = {
  theme: "dark",
  fontSize: "14px",
};

// Export the functions and variables
module.exports = {
  greet,
  calculate,
  settings,
};
