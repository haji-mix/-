// Define some functions and variables
const greet = () => {
  return "Hello, world!";
};

const calculate = (a, b) => {
  return a + b;
};

const settings = {
  theme: "dark",
  fontSize: "14px",
};

// Return the functions and variables as an object
{
  greet,
  calculate,
  settings,
}
